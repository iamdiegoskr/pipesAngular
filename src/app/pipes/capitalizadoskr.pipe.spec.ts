import { CapitalizadoskrPipe } from './capitalizadoskr.pipe';

describe('CapitalizadoskrPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizadoskrPipe();
    expect(pipe).toBeTruthy();
  });
});
